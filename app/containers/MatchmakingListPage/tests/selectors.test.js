// import { selectMatchmakingListPageDomain } from '../selectors';

describe('selectMatchmakingListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
