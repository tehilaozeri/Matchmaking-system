/*
 * AddPersonPage Messages
 *
 * This contains all the text for the AddPersonPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPersonPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPersonPage container!',
  },
});
