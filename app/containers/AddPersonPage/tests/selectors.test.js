// import { selectAddPersonPageDomain } from '../selectors';

describe('selectAddPersonPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
